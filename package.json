{
  "name": "last-base",
  "version": "0.24.0",
  "private": true,
  "description": "Last Base Stage 4: stable asset catalog, atlas metadata and lazy resource loading.",
  "scripts": {
    "build": "node tools/build.cjs",
    "check": "node tools/build.cjs --check && node qa/verify.cjs",
    "test": "node qa/run.cjs",
    "bench": "node qa/performance.cjs",
    "assets": "node tools/assets.cjs"
  },
  "devDependencies": {
    "@napi-rs/canvas": "0.1.100"
  },
  "engines": {
    "node": ">=20"
  }
}
