const fs=require('node:fs'),path=require('node:path'),assert=require('node:assert/strict');process.chdir(path.resolve(__dirname,'..'));process.env.LAST_BASE_ASSETS=process.cwd();
const {boot}=require('./audio-harness.cjs');
(async()=>{const h=boot(),{E,J}=h;await h.load();await E('Promise.all(Object.entries(AssetManifest.images).filter(([,d])=>!d.historical).map(([id])=>GameAssets.load(id)))');
 const out='qa/results/stage-d-visuals';fs.mkdirSync(out,{recursive:true});
 E("window.innerWidth=640;window.innerHeight=560;resizeCanvas();GameFlow.resume();menuOpen=false;scene='bunker';player.x=1210;player.y=675;V09Power.running=true;V09Power.fuel=100;for(const t of ['iron','wood','parts','concrete'])addItem(t,100);window.visualCraft=GamePlacement.request('furnace','craft');window.visualPlace=GamePlacement.request(visualCraft.instanceId,'place',GamePlacement.centered('furnace','reserve_l1',1950,400,1))");assert.equal(E('visualPlace.ok'),true);
 function room(){E("ctx.resetTransform();ctx.clearRect(0,0,1280,800);ctx.translate(-1610,-260);drawBunker();ctx.resetTransform()");return h.r.canvas.getContext('2d').getImageData(0,0,520,500).data.slice();}
 const installed=room();fs.writeFileSync(out+'/installed-room.png',h.r.canvas.toBuffer('image/png'));
 E("player.x=1950;player.y=475;window.pickTicket=GamePlacement.beginPickup('build:furnace:1')");assert.equal(E("pickTicket.ok"),true);h.r.advance(3001);assert.equal(E("GamePlacement.request('build:furnace:1','pack',null,pickTicket.token).ok"),true);const packed=room();fs.writeFileSync(out+'/packed-room.png',h.r.canvas.toBuffer('image/png'));
 const b=J("GameFootprints.body('build:furnace:1')");let changed=0;for(let y=Math.ceil(b.y-260+4);y<Math.floor(b.y-260+b.h-4);y++)for(let x=Math.ceil(b.x-1610+4);x<Math.floor(b.x-1610+b.w-4);x++){const p=(y*520+x)*4;if(Math.abs(installed[p]-packed[p])+Math.abs(installed[p+1]-packed[p+1])+Math.abs(installed[p+2]-packed[p+2])>25)changed++;}
 assert.ok(changed>4000,'full scene must render station over reserve floor: '+changed);
 E("GamePlacementUI.show('build:furnace:1');GamePlacementUI.move(1950,400);GamePlacementUI.rotate()");E('player.x=1900;player.y=510;for(let n=0;n<60;n++)updateCamera();draw()');fs.writeFileSync(out+'/placement-preview.png',h.r.canvas.toBuffer('image/png'));
 const report={version:require('../package.json').version,passed:4,failed:0,physicalBodyPixelsChanged:changed,files:['installed-room.png','packed-room.png','placement-preview.png'],errors:h.r.errors,limitation:'Native Canvas2D room/preview renders; not browser CSS or physical phone screenshots.'};assert.deepEqual(h.r.errors,[]);fs.writeFileSync('qa/results/stage-d-visuals.json',JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report));
})().catch(e=>{console.error(e);process.exitCode=1});
