# Immutable accepted R2 baseline

index.html, js/game.js and js/game.js.map are copied byte for byte from
LAST_BASE_0.30.1_Bunker_Level_1_Rework_R2.zip, accepted by the user on 2026-09-23.
The package metadata identifies that version. Assets are reused from the release
root by the test harness; Stage A does not modify them.

These files are test inputs, not the deployed runtime. Do not regenerate them.
