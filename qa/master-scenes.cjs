const fs=require('fs'),path=require('path');process.chdir(path.resolve(__dirname,'..'));const{setup}=require('./runtime.cjs');require(process.env.CODEX_PRIMARY_RUNTIME_NODE_MODULES+'/@napi-rs/canvas').GlobalFonts.registerFromPath('/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf','ReviewSans');
async function main(){const out='qa/results/master-player';fs.mkdirSync(out,{recursive:true});for(const [id,mode,w,h]of[['pc','PC',1280,800],['portrait','MOBILE',390,844],['landscape','MOBILE',844,390]]){const r=setup('index.html',{}, {width:w,height:h,maxTouchPoints:mode==='MOBILE'?5:0}),E=s=>r.eval(s);await E('Promise.all(Object.keys(AssetManifest.images).filter(id=>!id.startsWith("historical/")).map(id=>GameAssets.load(id)))');E(`GameInput.setMode('${mode}');scene='surface';menuOpen=false;playerDead=false;player.x=800;player.y=850;handSlots=[null,null,null,null];contextHand=null;activeHandSlot=0;player.aimX=0;player.aimY=1;moveX=0;moveY=1;movePower=1;navigation=null;V014Controls.stopRoute();V016Lighting.restore({schema:1,day:1,minute:840});V010Camera.restore({...V010Camera.capture(),zoom:.85});ActorVisuals.pose(null);`);for(let n=0;n<15;n++){r.advance(16.667);E('frameScale=1;updatePlayer();ActorVisuals.pose(null);');}E('draw()');fs.writeFileSync(`${out}/walk-${id}.png`,r.canvas.toBuffer('image/png'));E(`scene='bunker';player.x=1210;player.y=-115;movePower=0;player.moving=false;navigation=null;V014Controls.stopRoute();V09Power.running=true;V09Power.fuel=80;V010Camera.restore({...V010Camera.capture(),zoom:1.1});draw();`);fs.writeFileSync(`${out}/idle-bed-${id}.png`,r.canvas.toBuffer('image/png'));E(`V011Living.start('rest');draw();`);fs.writeFileSync(`${out}/sleep-${id}.png`,r.canvas.toBuffer('image/png'));if(r.errors.length)throw Error(JSON.stringify(r.errors));}
const r=setup('index.html'),E=s=>r.eval(s);await E('Promise.all(Object.keys(AssetManifest.images).filter(id=>id.startsWith("actor/")).map(id=>GameAssets.load(id)))');E(`ctx.setTransform(1,0,0,1,0,0);ctx.fillStyle='#1d2824';ctx.fillRect(0,0,1280,800);ctx.fillStyle='#e1e7d9';ctx.font='24px ReviewSans';ctx.fillText('IDLE',200,70);ctx.fillText('WALK',550,70);ctx.fillText('SLEEP',910,70);ctx.font='16px ReviewSans';ctx.fillText('Одинаковый игровой масштаб · увеличение 4× для сравнения',100,106);equipment.body=null;equipment.head=null;for(let i=0;i<3;i++){ctx.save();ctx.translate(230+i*360,230);ctx.scale(4,4);if(i===2)ActorVisuals.drawSleep({x:-49.5,y:-30,w:99,h:198},0);else ActorVisuals.renderPose({kind:'unarmed',id:i?ActorVisuals.config.body.unarmed:ActorVisuals.config.unarmed.idle,frame:0,item:null,hands:null},0,0,Math.PI/2);ctx.restore();}`);E(`ctx.fillStyle='#b8c5b4';ctx.font='17px ReviewSans';ctx.fillText('Голова ≈ 11 игровых единиц · плечи ≈ 29 · сон: прежние 4 кадра дыхания',100,704);ctx.fillText('Длина силуэта различается из-за вертикальной и лежачей позы.',100,740);`);fs.writeFileSync(`${out}/idle-walk-sleep-comparison.png`,r.canvas.toBuffer('image/png'));console.log('Captured9 game scenes +same-scale comparison; modeled PC/mobile, real Canvas2D');}
main().catch(e=>{console.error(e);process.exitCode=1});
